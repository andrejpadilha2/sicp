;; Create a global list which simply stores all assertions that didn't completed yet

;; once they are completed, remove it from the list

;; before starting any new assertion, check duplicates in that list

;; if there's a duplicate, stop, if there isn't, continue
